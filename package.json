{
  "name": "ambi",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate:fonts": "node scripts/font-catalog-generator.mjs",
    "dev": "vite",
    "build": "npm run generate:fonts && tsc && vite build",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.184.1",
    "typescript": "~6.0.2",
    "vite": "^8.0.12",
    "vitest": "^4.1.6"
  },
  "dependencies": {
    "manifold-3d": "^3.4.1",
    "three": "^0.184.0"
  }
}
